{
  "name": "cli-al-easy-admin-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn app.main:app --app-dir backend --reload --port 8000"
  },
  "workspaces": [
    "frontend"
  ]
}
